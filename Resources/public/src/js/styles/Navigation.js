import { lime, pink } from 'material-ui/colors';

export default {} = theme => {
    
    const appBarHeight = theme.spacing.unit * 7;
    
    return ({
        menuIcon: {
            textDecoration: 'none'
        }
    });
    
};
